# Synthetic key material for the automation-home row — see ../../README.md.
# Every value is the literal `ENC[FIXTURE]`, and no test here decrypts.
#
# Three blocks the ordinary row does not carry, and each is here for a reason
# a single-row fixture could not reach:
#
#   * the webhook secret and the Restate ingress pair, scoped by
#     `store::deployment::GITHUB_AUTOMATION_HOME_PROJECT` to this row alone;
#   * the DocuSign keys, demanded because this row's config.toml declares
#     DOCUSIGN_BASE_URL. `example-deployment` declines the integration and so
#     exercises the skip.
#   * OAUTH_MICROSOFT_CLIENT_SECRET, demanded because this row's config.toml
#     declares OAUTH_MICROSOFT_CLIENT_ID. `example-deployment` declines it too.
NAVIGATOR_SURREAL_USER: ENC[FIXTURE]
NAVIGATOR_SURREAL_PASSWORD: ENC[FIXTURE]
SESSION_SECRET: ENC[FIXTURE]
OAUTH_CLIENT_SECRET: ENC[FIXTURE]
OAUTH_MICROSOFT_CLIENT_SECRET: ENC[FIXTURE]
RESTATE_BROKER_URL: ENC[FIXTURE]
RESTATE_AUTH_TOKEN: ENC[FIXTURE]
RESTATE_INGRESS_URL: ENC[FIXTURE]
SENDGRID_API_KEY: ENC[FIXTURE]
SENDGRID_FROM_EMAIL: ENC[FIXTURE]
SENDGRID_INBOUND_SECRET: ENC[FIXTURE]
SENDGRID_EVENTS_SECRET: ENC[FIXTURE]
SENDGRID_EVENTS_PUBLIC_KEY: ENC[FIXTURE]
NAVIGATOR_GITHUB_APP_PRIVATE_KEY: ENC[FIXTURE]
NAVIGATOR_GITHUB_WEBHOOK_SECRET: ENC[FIXTURE]
NAVIGATOR_GITHUB_APP_LOGIN: ENC[FIXTURE]
NAVIGATOR_GIT_WRITER_TOKEN: ENC[FIXTURE]
DOCUSIGN_ACCOUNT_ID: ENC[FIXTURE]
DOCUSIGN_INTEGRATION_KEY: ENC[FIXTURE]
DOCUSIGN_USER_ID: ENC[FIXTURE]
DOCUSIGN_PRIVATE_KEY: ENC[FIXTURE]
DOCUSIGN_HMAC_KEY: ENC[FIXTURE]
DOCUSIGN_WEBHOOK_SECRET: ENC[FIXTURE]
sops:
    kms: []
    gcp_kms:
        - resource_id: projects/neon-law-stg/locations/us-west4/keyRings/navigator-secrets/cryptoKeys/deployment-config
          created_at: "2026-08-17T00:00:00Z"
          enc: FIXTURE
    version: 3.9.4
