# Vendored front-end assets — single source of truth.
#
# Every third-party CSS/JS/font we serve from `web/public/` is pinned here by
# version + SHA-256. The bytes on disk are the artifact; this file records what
# they are and where they came from. `web/tests/vendor_assets.rs` recomputes
# each `sha256` from `served_path` and fails on drift, so this file cannot lie.
#
# To refresh an asset, update the vendored bytes and this manifest together;
# never hand-edit a minified blob.
#
# Fields:
#   name         human label
#   version      pinned upstream version
#   upstream_url the exact URL the bytes were fetched from
#   served_path  path under web/public/ (also the URL after the `/public` nest)
#   sha256       SHA-256 of the on-disk bytes (enforced by the test)
#   modified     true if we edited the file post-download (its sha will NOT
#                match upstream; see `note`)

# Swagger UI, served at `/api` for the OIDC-gated `/api/*` read endpoints.
# `swagger-ui/index.html` and `swagger-ui/init.js` are ours (the Navigator
# banner, and the same-origin bootstrap that keeps the route's CSP at
# `script-src 'self'`), so they are first-party and not pinned here.

[[asset]]
name = "Swagger UI CSS"
version = "5.17.14"
upstream_url = "https://cdn.jsdelivr.net/npm/swagger-ui-dist@5.17.14/swagger-ui.css"
served_path = "swagger-ui/swagger-ui.css"
sha256 = "40170f0ee859d17f92131ba707329a88a070e4f66874d11365e9a77d232f6117"

[[asset]]
name = "Swagger UI JS bundle"
version = "5.17.14"
upstream_url = "https://cdn.jsdelivr.net/npm/swagger-ui-dist@5.17.14/swagger-ui-bundle.js"
served_path = "swagger-ui/swagger-ui-bundle.js"
sha256 = "c2e4a9ef08144839ff47c14202063ecfe4e59e70a4e7154a26bd50d880c88ba1"

[[asset]]
name = "Swagger UI standalone preset"
version = "5.17.14"
upstream_url = "https://cdn.jsdelivr.net/npm/swagger-ui-dist@5.17.14/swagger-ui-standalone-preset.js"
served_path = "swagger-ui/swagger-ui-standalone-preset.js"
sha256 = "33b7a6f5afcac4902fdf93281be2d2e12db15f241d384606e6e6d17745b7f86f"

[[asset]]
name = "Swagger UI favicon 16"
version = "5.17.14"
upstream_url = "https://cdn.jsdelivr.net/npm/swagger-ui-dist@5.17.14/favicon-16x16.png"
served_path = "swagger-ui/favicon-16x16.png"
sha256 = "af24ad604dd7b3bcda8f975ab973075f4a2f70a4087944a12f8ef8b63a3e07c2"

[[asset]]
name = "Swagger UI favicon 32"
version = "5.17.14"
upstream_url = "https://cdn.jsdelivr.net/npm/swagger-ui-dist@5.17.14/favicon-32x32.png"
served_path = "swagger-ui/favicon-32x32.png"
sha256 = "3ed612f41e050ca5e7000cad6f1cbe7e7da39f65fca99c02e99e6591056e5837"
